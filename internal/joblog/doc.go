// Package joblog provides a bounded, monotonically-sequenced log ring shared
// by the split pipeline and the library scan, and surfaced to the frontend
// over the status and scan HTTP endpoints.
package joblog
