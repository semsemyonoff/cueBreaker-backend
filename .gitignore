.DS_Store
.ralphex

# build artifacts
/cuebreaker
*.test
*.out

# the embedded SPA — the real bundle is baked in at image-build time; keep only
# the placeholder so //go:embed all:dist still builds.
web/dist/*
!web/dist/.gitkeep
!web/dist/index.html
